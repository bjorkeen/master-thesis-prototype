# Dependencies for the data-pipeline scripts in this folder:
#   generate_dataset.py  → numpy, pandas
#   train_model.py       → numpy, pandas, scikit-learn, joblib, shap, matplotlib
#   create_tables.py     → sqlalchemy (via data/db.py)
#
# These are SETUP-TIME dependencies (run once to build the dataset, model
# artefacts, and DB tables). The running FastAPI services declare their own
# runtime deps in services/*/requirements.txt.
#
# Install from the project root:
#   pip install -r data/requirements.txt

# Data processing — MUST match the versions the model was trained with so the
# joblib artefacts remain loadable by the ML Service.
pandas>=2.1.0
numpy>=1.26.0

# Machine learning — MUST match the version used by the ML Service at runtime.
scikit-learn>=1.6.0
joblib>=1.3.0

# Explainability — generates the SHAP plots in train_model.py.
shap>=0.51.0

# Plotting — confusion matrix + SHAP summary/waterfall PNGs (train_model.py).
matplotlib>=3.8.0

# ORM — schema definitions + table creation (db.py, create_tables.py).
sqlalchemy>=2.0.0

# Optional: only needed if you point DATABASE_URL at PostgreSQL instead of the
# default SQLite fallback. db.py imports psycopg2 lazily and falls back to
# SQLite if it is not installed.
# psycopg2-binary>=2.9.0
